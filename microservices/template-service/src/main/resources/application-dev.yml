spring:
  jpa:
    show-sql: true
  config:
    activate:
      on-profile: dev

logging:
  level:
    root: DEBUG
    com.pv.template: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

